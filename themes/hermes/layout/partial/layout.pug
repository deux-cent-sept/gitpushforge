doctype
html(lang=config.language)
    head
        include head
    body
        .wrap
            header
                if is_home()
                    include main_logo
                include nav
            main.container
                block container
            footer
                block pagination
                block copyright
        include scripts
